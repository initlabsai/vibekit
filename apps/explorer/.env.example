# Query service URL — where the Hono API is running
NEXT_PUBLIC_QUERY_SERVICE_URL=http://localhost:3001

# API key for authenticating with the query service (must match an API_KEYS entry)
NEXT_PUBLIC_QUERY_SERVICE_KEY=sk_dev_test123
