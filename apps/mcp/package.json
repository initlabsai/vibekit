{
  "name": "@initlabs/mcp-reference",
  "private": true,
  "version": "0.0.0",
  "description": "Reference deployment of @initlabs/mcp — copy this app to self-host your own VibeKit MCP.",
  "type": "module",
  "scripts": {
    "dev": "bun run src/stdio.ts",
    "dev:http": "bun run src/http.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@initlabs/core": "workspace:*",
    "@initlabs/mcp": "workspace:*",
    "algosdk": "3.7.0-beta.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
