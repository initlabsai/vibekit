# Generated by `bun run --cwd apps/cli bundle-skills`; reformatting it makes
# every regeneration a diff.
apps/cli/src/skills/bundled.ts

# Test corpora and fixtures — byte-for-byte recordings, not source.
packages/explorer/test/recorded/
packages/vibekit/test/tools/fixtures/

# Regenerated by `next build` in its own style on every run; the file says so
# itself. Formatting it just makes the next build a diff.
apps/web/next-env.d.ts

# Build output and generated manifests.
**/dist/
**/.next/
**/.astro/
verify/packed-consumer/package.json

# Prose is hand-wrapped and hand-tended. Prettier pads markdown tables to
# fixed widths and mangles the alignment on em-dashes; it buys nothing here.
*.md
