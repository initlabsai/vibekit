---
'@initlabs/vibekit': patch
---

`apps/reference`: the reference agent — a Bun server mounting `createAgentHandler`, `createActionRoutes`, and `createRestHandler`, and a Vite/React page over the `vibekit add` components. About 200 lines of app; the tutorial's endpoint.
