---
'@initlabs/vibekit': patch
---

Type-check the test suites, fix the `searchAssetBalances` return type (it always returned `decimals`), rename the MCP adapter's internal `resolveDeployment` to `resolveMcpDeployment`, and describe the code as shipped in comments.
