---
'@initlabs/vibekit-agent': minor
---

The links she posts: seven public entity routes — /transaction, /group,
/asset, /application, /block, /address, /nfd — each rendering a branded
1200×630 OG poster for crawlers and a summary page for humans, straight
from chain state. Transactions resolve indexer-first with an algod
pending-pool rescue and cache headers that never let a miss stick;
groups fit compact rows on one card; addresses carry their primary NFD
name, and /nfd is a second door into the same card. The brand is qt314:
QT314 AGENT on every surface, qt314.ai as the canonical home.
