{
  "name": "vibekit",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "cli": "turbo run build --filter=@initlabs/vibekit-cli^... && bun run --cwd apps/cli dev",
    "tui": "turbo run build --filter=@initlabs/vibekit-tui^... && bun run --cwd apps/tui dev",
    "web": "turbo run build --filter=@initlabs/vibekit-web^... && bun run --cwd apps/web dev",
    "website": "bun run --cwd apps/website dev",
    "build:website": "turbo run build --filter=@initlabs/vibekit-website",
    "mcp": "turbo run build --filter=@initlabs/vibekit... && bun packages/vibekit/examples/stdio.ts",
    "verify:packed": "bun verify/verify-packed.ts",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "prettier": "^3.9.6",
    "turbo": "^2.10.11",
    "typescript": "^5.9.3"
  }
}
